<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <record id="res_config_settings_view_form_sale_override_credit_limit" model="ir.ui.view">
        <field name="name">res.config.settings.sale.view.form.over.ride.credit.limit</field>
        <field name="model">res.config.settings</field>
        <field name="inherit_id" ref="sale.res_config_settings_view_form"/>
        <field name="arch" type="xml">
            <xpath expr="//block[@name='catalog_setting_container']" position="after">
                <block title="Over Ride Credit Limit Approval or Reject Rule" name="override_credit_limit_approval_container">
                    <setting
                        id="override_credit_limit_approval"
                        string="Override Credit Limit Approval or Reject Rule"
                        help="Allow users to bypass credit limit approval  or reject restrictions."                    >
                        <field name="override_credit_limit_approval"/>
                        <div class="content-group" invisible="not override_credit_limit_approval">
                            <div class="mt8 text-muted">
                                When enabled, users will be allowed to approve or reject credit limit orders even without manager rights.
                            </div>
                        </div>
                    </setting>
                </block>
            </xpath>
        </field>
    </record>
</odoo>
