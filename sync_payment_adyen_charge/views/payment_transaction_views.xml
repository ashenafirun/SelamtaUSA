<?xml version="1.0" encoding="utf-8"?>
<odoo>

    <record id="payment_transaction_form_inherit_sync_payment_charge" model="ir.ui.view">
        <field name="name">Payment transaction Form</field>
        <field name="model">payment.transaction</field>
        <field name="inherit_id" ref="payment.payment_transaction_form" />
        <field name="arch" type="xml">
            <field name="source_transaction_id" position="after">
                <field name="fees" invisible="fees == 0.0" />
                <field name="net_amount" invisible="fees == 0.0" />
            </field>
        </field>
    </record>

    <record id="payment_transaction_tree_inherit_sync_payment_charge" model="ir.ui.view">
        <field name="name">Payment transaction List</field>
        <field name="model">payment.transaction</field>
        <field name="inherit_id" ref="payment.payment_transaction_list" />
        <field name="arch" type="xml">
            <field name="amount" position="after">
                <field name="fees" />
            </field>
        </field>
    </record>

</odoo>
